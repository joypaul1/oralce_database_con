1. LOGIN BY SYS ADMIN IN (SQL OR CMD)

2. Viewing Existing Oracle Directory Objects To list all existing directory objects in your database, use:

sql
### 
SELECT directory_name, directory_path FROM dba_directories;

